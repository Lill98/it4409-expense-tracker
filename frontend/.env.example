# Sao chép thành .env khi cần. KHÔNG commit file .env.

# Để RỖNG khi chạy dev: Vite proxy /api sang http://localhost:3001
# (xem cấu hình proxy trong vite.config.js).
#
# Khi build để deploy, trỏ tới domain thật của backend, ví dụ:
#   VITE_API_BASE_URL=https://it4409-expense-api.onrender.com
VITE_API_BASE_URL=
